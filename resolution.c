/*
#include<stdio.h>
#include<stdlib.h>

int len(char *string)
{
    int i = 0;
    while(string[i] != '\0') i++;
    return i;
}

void inverte(char *string)
{
    printf("%d \n", len(string));
}

int main()
{

    char **string;
    printf("Digite a string: ");
    scanf("%s",string);
 
    printf("%s", &string);

    //inverte(string);

    return 0;
}
*/
